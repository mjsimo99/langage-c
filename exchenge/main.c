#include<stdio.h>
int main(){
int n;
int tamp=0;
printf("donner un valeur de 4 numbers\n");
scanf("%d",&n);
while (n != 0)
   {
      N_In = temp * 10;
      N_In = temp + n%10;
      n= n/10;
   }
   printf(" La remplacement est %d",perm);
}
